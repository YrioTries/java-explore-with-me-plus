server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://localhost:5436/main
    username: dbuser
    password: 12345
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: create-drop
      dialect: org.hibernate.dialect.PostgreSQLDialect
    show-sql: false